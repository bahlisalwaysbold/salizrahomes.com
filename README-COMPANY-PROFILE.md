# Company Profile Page

A new `company-profile.html` page has been added without changing the existing visual theme.

## Replace the document placeholders
Open `documents/` and replace these files with the client-approved documents, keeping the same filenames:

- `company-profile.pdf` — full company profile
- `credentials-pack.pdf` — certificates / registrations / compliance documents
- `project-experience.pdf` — project experience / selected works booklet

The buttons on the page already point to those filenames. No HTML changes are required when you simply replace the PDFs.

## Replace the image placeholders
The page intentionally uses the existing site placeholder system. Replace the marked blocks with final photography when ready.

## What was added
- Company profile hero / document card
- Animated metric strip
- Overview cards
- Company story section
- Capability pillars
- Delivery timeline animation
- Leadership profile slot
- Document room with view + download actions
- Values / standards section
- Final conversion CTA
- Reduced-motion support for accessibility
- Mobile-friendly hover / reveal / count animations
